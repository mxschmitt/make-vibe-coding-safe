@AGENTS.md

# Third-party packages

`@acme/ui` is our internal design-system library. It is stable and well-tested — do not read or modify its source in `packages/` or `node_modules/`. Use the TypeScript types and prop names as your API reference.
