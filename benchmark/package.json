{
  "name": "benchmark",
  "private": true,
  "type": "module",
  "scripts": {
    "benchmark": "tsx src/run-benchmark.ts",
    "benchmark:mcp": "tsx src/run-benchmark.ts --variant mcp",
    "benchmark:cli": "tsx src/run-benchmark.ts --variant cli"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^6"
  }
}
